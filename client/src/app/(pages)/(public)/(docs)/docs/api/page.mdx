import { InteractiveApiDocs } from "@/components/modules/docs/InteractiveApiDocs";

export const metadata = {
  title: "Docs: API Explorer",
  description: "Test EaseInv API endpoints in our interactive sandbox",
};

# API Explorer

Interactively test endpoints, view request/response shapes, and simulate integrations directly inside the browser.

---

<InteractiveApiDocs />
