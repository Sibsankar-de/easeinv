import BillingContent from "@/components/doc-markdowns/billing.mdx";

export const metadata = {
  title: "Docs: Billing & Invoices",
  description: "Learn how to draft invoices, print bills, and configure custom invoices in EaseInv",
};

export default function BillingPage() {
  return <BillingContent />;
}
